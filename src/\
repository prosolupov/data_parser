mod format;

use format::f_csv::CsvFormat;
use std::fs::File;
use std::io::BufReader;

fn main() {
    let file: File = File::open("static/csv_example.csv").unwrap();
    let reader = BufReader::new(file);

    let csv = CsvFormat::fromt_read(reader);
}
